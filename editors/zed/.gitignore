# Rust/wasm build output for the Zed extension (regenerated by `make build-zed`).
/target/
# tree-sitter-ember is its own git repo; its node_modules never belong to the main tree.
tree-sitter-ember/node_modules/
