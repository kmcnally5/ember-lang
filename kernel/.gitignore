# Generated by `make kernel` (docs/design/kernel-freestanding.md) — the source is checked in
# (hello.em, rt.c, ember_rt.h, boot.S, kernel.ld); these build artifacts are not.
hello.c
*.o
*.elf
emit.err
