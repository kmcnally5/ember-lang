# doc_comments

_Generated by `emberc --emit=docs` from `///` doc comments._

## Point

```ember
struct Point
```

A 2-D point in the plane.
Copied by value; both fields are public.

**Fields**

- `x: int` — The horizontal coordinate.
- `y: int` — The vertical coordinate.

#### `fn dist2(self) -> int`

The squared distance from the origin.
Avoids a sqrt when only ordering matters.

## Light

```ember
enum Light
```

The colours a traffic light can show.

**Variants**

- `Red` — Stop.
- `Amber` — Get ready.
- `Green` — Go.

## Shape

```ember
interface Shape
```

A thing that can report its area.

#### `fn area(self) -> float`

The area enclosed by the shape.

## add

```ember
fn add(a: int, b: int) -> int
```

Add two integers and return their sum.

## max_width

```ember
let max_width
```

The maximum supported width, in pixels.
